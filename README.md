# Usage

1. Build the docker image via `build.sh`
2. Put the source `*.ttf` file(s) into the project root folder
3. (Optional) Modify the whitelisted characters in `run.sh`
4. Run `run.sh` to generate the subsetted `.woff2` file(s)
